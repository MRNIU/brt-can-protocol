<!-- Copyright The brt-can-protocol Contributors -->
<!-- 本文件记录供应商 PDF 的已确认勘误、未确认边界和库裁决。 -->

# 《拉绳位移传感器 CAN 通信 V2.7》勘误

整理日期：2026-09-15。

## 依据与范围

本勘误针对供应商 PDF [《拉绳位移传感器 CAN 通信 V2.7》](https://www.buruiter.com/wp-content/uploads/2025/10/004-%E6%8B%89%E7%BB%B3%E4%BD%8D%E7%A7%BB%E4%BC%A0%E6%84%9F%E5%99%A8CAN%E9%80%9A%E4%BF%A1-V2.7.pdf) 印刷页 16–19，对应 PDF 阅读器的物理页 18–21。结论来自本仓库用户转述的厂商邹工答复；没有回复日期、适用产品型号、固件版本或总线抓包。因此，本文不把 PDF 的 V2.7 或指令表 V2.1 当作已确认固件版本，也不把下列示例当作实板通信证据。

引用的厂商原话如下；标点和大小写只作排版整理：

> “发送04 07没要求 回复是07 这里说明书描述有问题”
>
> “按照示例来哈 这个是高位在前，大端”
>
> “can总线目前的指令分为了两种 小端和大端都有，按照示例来哈 这个是大端”
>
> “以示例为准 大端”
>
> “19页写错了 准确的是125K”

厂商对 `0x22` 的“高位在前，大端”答复同时适用于设置地址请求和 32 位应答状态；`0x0B` 和 `0x0D` 的请求也以示例为准使用大端。

## `0x22` 设置扩展地址（印刷页 16）

PDF 将该请求和应答都写成 7 个数据字节，却在示例中写 `LEN=04`。厂商确认请求的 `LEN=04` 或 `LEN=07` 都不作要求，应答为 `LEN=07`。

这里的 CAN DLC 与协议 `LEN` 不同：DLC 是 CAN 帧控制字段中的实际数据字节数；`LEN` 是 `DATA[0]` 中、包含自身的协议字段。`0x22` 的兼容请求始终具有 DLC 7，即使 `DATA[0]` 为 `04`。本库解码这两个请求写法，编码统一使用与 DLC 一致的 `LEN=07`。应答只接受 `LEN=07`、DLC 7。

厂商确认设置地址请求和 32 位应答状态均按示例使用大端。因此 `0x18fff225` 的请求 DATA 是 `18 ff f2 25`，不是 `25 f2 ff 18`。

厂商还说明扩展地址模式下应答第二个字节的 DEVICE_ID 因只保留一个字节，“取 ID 最后一个字节”。例如扩展 CAN ID `0x18fff225` 对应 DEVICE_ID `0x25`。该说明没有给出适用型号或固件范围；本库仍要求调用方显式构造 `Address::extended(0x18ff_f225, 0x25)`，不自动推导或强制验证这个关系，也不推定广播语义。

`0x22` 应答的四字节状态字段使用大端 `Status32` 编解码。PDF 定义 `0` 为成功、其他值为错误码，但未列出具体错误码含义；库保留完整 32 位原始值。

## `0x0B` 设置速度采样时间（印刷页 17）

PDF 的通用规则称多字节字段低字节在前，但该命令示例以 `03 e8` 表示 1000 ms。厂商确认“以示例为准，大端”。因此 `0x03e8` 的请求 DATA 为 `03 e8`。

## `0x0D` 设置当前位置计数（印刷页 18）

PDF 的通用规则同样与该命令示例冲突。厂商确认“以示例为准，大端”。因此 `0x00012345` 的请求 DATA 为 `00 01 23 45`。

## 波特率值 `0x03`

印刷页 16 的 `0x03` 命令表将值 `03` 定义为 125 kbps，印刷页 19 的 LED 表却写成 150K。厂商确认第 19 页错误：值 `0x03` 是 **125 kbps**。

## 规范帧示例

下表由 PDF 示例和上述厂商答复整理，方便使用本库时检查 CAN 标识符格式、DLC 和全部有效数据字节；它们不是抓包。所有示例均为 Classic CAN 数据帧（RTR=0、非 FD）。`0x22` 示例按 PDF 的默认旧地址发出，成功后按 PDF 说明使用新扩展地址应答。

| 命令／方向 | CAN ID 格式与值 | DLC | DATA（全部有效字节） | 说明 |
|---|---|---:|---|---|
| `0x22` 请求 | 标准（11 位）`0x001` | 7 | `07 01 22 18 FF F2 25` | 规范编码；设备也接受首字节 `04` 的同一 DLC 7 请求。 |
| `0x22` 应答 | 扩展（29 位）`0x18FFF225` | 7 | `07 25 22 00 00 00 00` | PDF 成功状态示例；32 位状态为大端。 |
| `0x0B` 请求 | 标准（11 位）`0x001` | 5 | `05 01 0B 03 E8` | 1000 ms，大端。 |
| `0x0B` 应答 | 标准（11 位）`0x001` | 4 | `04 01 0B 00` | PDF 成功状态示例。 |
| `0x0D` 请求 | 标准（11 位）`0x001` | 7 | `07 01 0D 00 01 23 45` | 74565，大端。 |
| `0x0D` 应答 | 标准（11 位）`0x001` | 4 | `04 01 0D 00` | PDF 成功状态示例。 |
| `0x03` 请求 | 标准（11 位）`0x001` | 4 | `04 01 03 03` | 设置 125 kbps。 |
| `0x03` 应答 | 标准（11 位）`0x001` | 4 | `04 01 03 00` | PDF 成功状态示例。 |

## 升级影响

- `Request::SetExtendedAddress`、`Request::SetSpeedSampleTime` 和 `Request::SetPosition` 的多字节请求参数改为大端；`Response::SetExtendedAddress(Status32)` 的 32 位状态在线上也改为大端。原先按 PDF 通用小端规则编码或解码的调用方必须更新测试向量和总线预期。
- `decode_request` 对 `0x22` 的 DLC 7 请求额外兼容 `LEN=04`；`encode_request` 仍只生成 `LEN=07`。其他命令及 `0x22` 应答继续要求 `LEN` 与 DLC 相等。
- `Address::extended`、`Response::SetExtendedAddress(Status32)` 和波特率 `BaudRate::Kbps125` 的公开 API 类型不变，未知状态仍保留完整数据位。
- 本仓库的测试只验证纯软件编解码；没有执行设备设址、持久化或通信速率切换。
